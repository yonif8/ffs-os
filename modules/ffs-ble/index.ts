// ffs-ble — local Expo native module: from-scratch CoreBluetooth G2 driver
// (Phase 2 — dual-radio L/R sync manager).
//
// Public entry point. Re-exports the typed native module + all event types.

export { default } from "./src/FfsBleModule";
export * from "./src/FfsBleModule";
